import { Layout } from 'antd';
import React from 'react';
const { Header } = Layout;

const Head = () => {
    return (
        <Header
        className="site-layout-sub-header-background"
        style={{
        padding: 0,
        }}
        />
    )
};

export default Head;